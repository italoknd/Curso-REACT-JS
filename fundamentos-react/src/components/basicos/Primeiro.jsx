import React from 'react'

function Primeiro(){
  const msg = 'Seja bem vindo ao JSX!'

  return (
    <div>
      <h2>Primeiro Componente</h2>
      <p>{ msg }</p>
    </div>
  )
}

export default Primeiro;